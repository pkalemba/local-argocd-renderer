# Anchored to the repo root: unanchored, this also matched the
# cmd/local-argocd-renderer directory and silently ignored new files in it.
/local-argocd-renderer

# Devenv
.devenv*
devenv.local.nix

# direnv
.direnv

# pre-commit
.pre-commit-config.yaml

# Build output
dist/

# Node (semantic-release)
node_modules/
